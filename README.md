# CleanText

Мощный онлайн инструмент для очистки и обработки текста. Бесплатное приложение с поддержкой PWA, которое работает полностью в браузере без отправки данных на сервер.

## ✨ Особенности

- 🔒 **Полная конфиденциальность** - весь текст обрабатывается локально в браузере
- 📱 **PWA поддержка** - можно установить как приложение на устройство
- 🌓 **Темная/светлая тема** - переключение между темами с сохранением в localStorage
- 🎯 **12 режимов обработки** - от удаления пробелов до преобразования переносов строк
- 🚀 **SEO-оптимизация** - отдельные страницы для каждой операции с примерами и FAQ
- 📱 **Адаптивный дизайн** - отлично работает на всех устройствах
- ⚡ **Быстрая работа** - мгновенная обработка текста любого размера

## 🛠️ Доступные инструменты

### Основные функции:
- **Убрать лишние пробелы** - удаляет множественные пробелы, оставляя одинарные
- **Убрать пробелы в начале и в конце строки** - очищает края каждой строки
- **Убрать пустые строки** - удаляет все пустые строки из текста
- **Заменить переносы строк на пробелы** - склеивает строки в одну линию
- **Убрать все переносы строк** - создает непрерывную строку
- **Убрать все пробелы и переносы строк** - полностью компактный текст

### Дополнительные функции:
- **Убрать все пробелы** - удаляет только пробелы, сохраняя переносы строк
- **Убрать двойные пробелы** - нормализует множественные пробелы
- **Заменить пробелы на переносы строк** - разбивает текст на строки
- **Убрать пустые строки и строки с пробелами** - агрессивная очистка
- **Убрать только большие пробелы** - удаляет только явно лишние пробелы
- **Заменить большие пробелы на маленькие** - нормализует пробелы

## 🚀 Быстрый старт

### Установка зависимостей

```bash
# Используя pnpm (рекомендуется)
pnpm install

# Или используя npm
npm install

# Или используя yarn
yarn install
```

### Запуск в режиме разработки

```bash
pnpm dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Сборка для production

```bash
pnpm build
pnpm start
```

## 📁 Структура проекта

```
cleantext/
├── app/
│   ├── [mode]/              # Динамические страницы для каждой операции
│   │   ├── page.tsx          # Серверный компонент страницы
│   │   └── ModePageClient.tsx # Клиентский компонент страницы
│   ├── components/           # React компоненты
│   │   ├── Button.tsx        # Переиспользуемая кнопка
│   │   ├── Header.tsx        # Шапка сайта
│   │   ├── Footer.tsx        # Подвал сайта
│   │   ├── TextProcessor.tsx # Основной компонент обработки текста
│   │   ├── SEOContent.tsx    # SEO контент (примеры, FAQ)
│   │   ├── OperationsList.tsx # Список всех операций
│   │   ├── PWAInstallButton.tsx # Кнопка установки PWA
│   │   └── ServiceWorker.tsx # Регистрация Service Worker
│   ├── utils/
│   │   ├── textProcessor.ts  # Логика обработки текста
│   │   └── seoData.ts        # SEO метаданные для каждой операции
│   ├── types/
│   │   └── index.ts          # TypeScript типы
│   ├── layout.tsx            # Корневой layout
│   ├── page.tsx              # Главная страница
│   ├── sitemap.ts            # Генерация sitemap.xml
│   └── robots.ts              # Генерация robots.txt
├── public/
│   ├── sw.js                 # Service Worker
│   ├── manifest.json         # PWA манифест
│   └── *.svg                 # Иконки приложения
└── package.json
```

## 🎨 Технологии

- **Next.js 16** - React фреймворк с App Router
- **React 19** - UI библиотека
- **TypeScript** - типизированный JavaScript
- **Tailwind CSS 4** - утилитарный CSS фреймворк
- **Lucide React** - иконки
- **PWA** - Progressive Web App функциональность

## 🔧 Настройка

### Переменные окружения

Создайте файл `.env.local` для настройки:

```env
NEXT_PUBLIC_SITE_URL=https://your-domain.com
```

Этот URL используется в sitemap.xml и robots.txt.

### PWA настройка

PWA настройки находятся в `public/manifest.json`. Вы можете изменить:
- Название приложения
- Описание
- Цвета темы
- Иконки

## 📱 PWA функциональность

Приложение поддерживает установку как PWA:

1. **Автоматическое обнаружение** - браузер автоматически предлагает установку
2. **Кнопка установки** - доступна в шапке и подвале сайта
3. **Офлайн работа** - Service Worker кэширует основные ресурсы
4. **Standalone режим** - работает как нативное приложение

## 🔍 SEO оптимизация

Каждая операция имеет отдельную страницу с:
- Уникальными метаданными (title, description)
- Структурированным H1 заголовком
- Примерами "до/после" обработки
- FAQ секцией с ответами на вопросы
- Информацией о безопасности

Автоматически генерируются:
- `sitemap.xml` - карта сайта для поисковых систем
- `robots.txt` - инструкции для поисковых роботов

## 🎯 Использование

1. Выберите режим обработки текста
2. Вставьте или введите текст в поле "Исходный текст"
3. Результат появится автоматически в поле "Результат"
4. Скопируйте обработанный текст кнопкой "Копировать результат"

Или перейдите на страницу конкретной операции для более подробной информации и примеров.

## 🔒 Безопасность и конфиденциальность

- ✅ Весь текст обрабатывается локально в браузере
- ✅ Данные не отправляются на сервер
- ✅ Никакая информация не сохраняется
- ✅ Нет трекинга и аналитики
- ✅ Полная конфиденциальность ваших данных

## 📝 Скрипты

```bash
# Разработка
pnpm dev

# Сборка
pnpm build

# Production запуск
pnpm start

# Линтинг
pnpm lint
```

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 📧 Поддержка

Если у вас есть вопросы или предложения, свяжитесь с нами через email: support@cleantext.com

---

**CleanText** - Чистый текст, чистая мысль! 🚀

